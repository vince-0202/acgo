# FinishAt 2026-03-17

### `/` 命令系统扩展

- [x] 基础命令实现
- [x] 设计命令注册机制：
    - [x] 使用一个简单的 registry（map[name]CommandHandler），便于日后在代码或扩展中注册新命令。
- [x] 扩展更多命令（如 `/new` 新会话、`/tree` 会话树、`/name` 等），与 pi-mono 对齐。

### 配置与上下文文件体系（扩展）

- [x] 基础上下文文件加载与合成
- [x] 提供一个简单命令（或按键）展示当前生效的 system prompt 摘要。
- [x] 支持热重载或 `/reload` 重新读取 context 文件（可选）。